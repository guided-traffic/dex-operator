---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: dexoauth2connectors.dex.gtrfc.com
spec:
  group: dex.gtrfc.com
  names:
    kind: DexOAuth2Connector
    listKind: DexOAuth2ConnectorList
    plural: dexoauth2connectors
    shortNames:
    - dexoauth2
    singular: dexoauth2connector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.installationRef.name
      name: Installation
      type: string
    - jsonPath: .spec.authorizationURL
      name: Auth URL
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: DexOAuth2Connector is the Schema for the dexoauth2connectors
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              DexOAuth2ConnectorSpec defines configuration for the generic Dex OAuth2
              connector.
            properties:
              authorizationURL:
                description: AuthorizationURL is the provider's authorization endpoint.
                format: uri
                type: string
              claimMapping:
                description: ClaimMapping maps provider claim fields to Dex claim
                  fields.
                properties:
                  email:
                    description: Email maps a provider claim to the email claim.
                    type: string
                  groups:
                    description: Groups maps a provider claim to the groups claim.
                    type: string
                  preferredUsername:
                    description: PreferredUsername maps a provider claim to preferred_username.
                    type: string
                type: object
              clientIDRef:
                description: ClientIDRef references the Secret key holding the OAuth2
                  client ID.
                properties:
                  key:
                    description: Key within the Secret.
                    type: string
                  name:
                    description: Name of the Secret resource.
                    type: string
                required:
                - key
                - name
                type: object
              clientSecretRef:
                description: |-
                  ClientSecretRef references the Secret key holding the OAuth2 client
                  secret.
                properties:
                  key:
                    description: Key within the Secret.
                    type: string
                  name:
                    description: Name of the Secret resource.
                    type: string
                required:
                - key
                - name
                type: object
              displayName:
                description: DisplayName is the human-readable connector name shown
                  on the Dex login page.
                type: string
              headerPrefix:
                description: HeaderPrefix is prepended to the Authorization header
                  value.
                type: string
              id:
                description: ID is the connector ID used internally by Dex. Defaults
                  to metadata.name.
                type: string
              insecureTrustEmail:
                description: InsecureTrustEmail trusts the email claim without verifying
                  email_verified.
                type: boolean
              installationRef:
                description: InstallationRef references the DexInstallation this connector
                  belongs to.
                properties:
                  name:
                    description: Name of the DexInstallation resource.
                    type: string
                  namespace:
                    description: Namespace where the DexInstallation resides.
                    type: string
                required:
                - name
                - namespace
                type: object
              redirectURI:
                description: RedirectURI is the callback URL registered with the OAuth2
                  provider.
                type: string
              rootCARef:
                description: RootCARef references the Secret key holding a CA bundle
                  for the provider.
                properties:
                  key:
                    description: Key within the Secret.
                    type: string
                  name:
                    description: Name of the Secret resource.
                    type: string
                required:
                - key
                - name
                type: object
              scopes:
                description: Scopes lists the OAuth2 scopes to request.
                items:
                  type: string
                type: array
              tokenURL:
                description: TokenURL is the provider's token endpoint.
                format: uri
                type: string
              userInfoURL:
                description: UserInfoURL is the optional provider's user info endpoint.
                type: string
            required:
            - authorizationURL
            - clientIDRef
            - clientSecretRef
            - displayName
            - installationRef
            - tokenURL
            type: object
          status:
            description: DexOAuth2ConnectorStatus defines the observed state of DexOAuth2Connector.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the generation last processed by
                  the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
