---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: dexoidcconnectors.dex.gtrfc.com
spec:
  group: dex.gtrfc.com
  names:
    kind: DexOIDCConnector
    listKind: DexOIDCConnectorList
    plural: dexoidcconnectors
    shortNames:
    - dexoidc
    singular: dexoidcconnector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.installationRef.name
      name: Installation
      type: string
    - jsonPath: .spec.issuer
      name: Issuer
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: DexOIDCConnector is the Schema for the dexoidcconnectors API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DexOIDCConnectorSpec defines configuration for the Dex OIDC
              connector.
            properties:
              acrValues:
                description: ACRValues sets the acr_values parameter in the auth request.
                items:
                  type: string
                type: array
              basicAuthUnsupported:
                description: BasicAuthUnsupported forces HTTP Basic Auth in token
                  requests.
                type: boolean
              claimMapping:
                description: ClaimMapping maps upstream claims to Dex claims.
                properties:
                  email:
                    description: Email is the upstream claim mapped to the email claim.
                    type: string
                  groups:
                    description: Groups is the upstream claim mapped to the groups
                      claim.
                    type: string
                  preferredUsername:
                    description: PreferredUsername is the upstream claim mapped to
                      preferred_username.
                    type: string
                type: object
              clientIDRef:
                description: ClientIDRef references the Secret key holding the OIDC
                  client ID.
                properties:
                  key:
                    description: Key within the Secret.
                    type: string
                  name:
                    description: Name of the Secret resource.
                    type: string
                required:
                - key
                - name
                type: object
              clientSecretRef:
                description: ClientSecretRef references the Secret key holding the
                  OIDC client secret.
                properties:
                  key:
                    description: Key within the Secret.
                    type: string
                  name:
                    description: Name of the Secret resource.
                    type: string
                required:
                - key
                - name
                type: object
              discoveryPollInterval:
                description: |-
                  DiscoveryPollInterval overrides how often the discovery document is
                  refreshed (Go duration string, e.g. "30m").
                type: string
              displayName:
                description: DisplayName is the human-readable connector name shown
                  on the Dex login page.
                type: string
              getUserInfo:
                description: GetUserInfo fetches additional claims from the userinfo
                  endpoint.
                type: boolean
              hostedDomains:
                description: |-
                  HostedDomains restricts authentication to users from the listed
                  Google-hosted domains (relevant for Google IdPs).
                items:
                  type: string
                type: array
              id:
                description: ID is the connector ID used internally by Dex. Defaults
                  to metadata.name.
                type: string
              insecureEnableGroups:
                description: |-
                  InsecureEnableGroups enables group claim support even when the IdP does
                  not include it in the discovery document.
                type: boolean
              insecureSkipEmailVerified:
                description: InsecureSkipEmailVerified skips the email_verified claim
                  check.
                type: boolean
              installationRef:
                description: InstallationRef references the DexInstallation this connector
                  belongs to.
                properties:
                  name:
                    description: Name of the DexInstallation resource.
                    type: string
                  namespace:
                    description: Namespace where the DexInstallation resides.
                    type: string
                required:
                - name
                - namespace
                type: object
              issuer:
                description: Issuer is the OIDC issuer URL of the upstream IdP.
                format: uri
                type: string
              overrideClaimMapping:
                description: OverrideClaimMapping enables overriding claim mapping
                  with ClaimMapping.
                type: boolean
              promptType:
                description: PromptType overrides the prompt parameter sent to the
                  upstream IdP.
                type: string
              redirectURI:
                description: RedirectURI is the callback URL registered with the upstream
                  IdP.
                type: string
              rootCARef:
                description: |-
                  RootCARef references the Secret key holding a CA bundle for the upstream
                  IdP's HTTPS endpoint.
                properties:
                  key:
                    description: Key within the Secret.
                    type: string
                  name:
                    description: Name of the Secret resource.
                    type: string
                required:
                - key
                - name
                type: object
              scopes:
                description: |-
                  Scopes is the list of additional scopes to request from the IdP.
                  openid is always included.
                items:
                  type: string
                type: array
              userIDKey:
                description: UserIDKey is the claim name used as the user's stable
                  ID.
                type: string
              userNameKey:
                description: UserNameKey is the claim name used as the username.
                type: string
            required:
            - clientIDRef
            - clientSecretRef
            - displayName
            - installationRef
            - issuer
            type: object
          status:
            description: DexOIDCConnectorStatus defines the observed state of DexOIDCConnector.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the generation last processed by
                  the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
